# Test configuration for googleai_dart
#
# Integration tests require GOOGLE_GENAI_API_KEY environment variable.
# Run with: dart test --tags=integration

tags:
  integration:
    # Register the integration tag to suppress warnings
