# Test configuration for vertex_ai
#
# Integration tests require Google Cloud credentials.
# Run with: dart test --tags=integration

tags:
  integration:
    # Register the integration tag to suppress warnings
