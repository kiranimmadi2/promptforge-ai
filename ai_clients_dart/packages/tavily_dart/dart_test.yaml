# Test configuration for tavily_dart
#
# Integration tests require TAVILY_API_KEY environment variable.
# Run with: dart test --tags=integration

tags:
  integration:
    # Register the integration tag to suppress warnings
