// Agent versions use the same [Agent] type.
//
// The `ListAgentVersionsResponse` returns a list of [Agent] objects
// representing different versions. No separate AgentVersion class is
// needed — see `agent_list_response.dart`.
