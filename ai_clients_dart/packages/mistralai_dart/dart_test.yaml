# Test configuration for mistralai_dart
#
# Integration tests require MISTRAL_API_KEY environment variable.
# Run with: dart test --tags=integration

tags:
  integration:
    # Register the integration tag to suppress warnings
